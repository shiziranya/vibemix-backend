# 主线/支线功能实现说明

## 功能概述

实现了地图探索系统的主线/支线功能，支持节点和边的分类，以及基于节点状态的边解锁逻辑。

## 已完成的工作

### 1. 数据模型更新

#### MapNode 模型新增字段
- `path_role` (VARCHAR(10), NOT NULL, DEFAULT 'main')
  - 节点类型：`'main'`（主线节点）或 `'side'`（支线节点）
  - 添加了约束检查 `CHECK (path_role IN ('main', 'side'))`

- `unlock_by` (INTEGER, NULL, FOREIGN KEY -> cocktails.id)
  - 支线节点解锁所需的鸡尾酒ID
  - 当用户完成该鸡尾酒时，支线节点自动解锁

#### MapEdge 模型新增字段
- `path_role` (VARCHAR(10), NOT NULL, DEFAULT 'main')
  - 边类型：`'main'`（主线关系）或 `'side'`（支线关系）
  - 添加了约束检查 `CHECK (path_role IN ('main', 'side'))`

### 2. 数据库迁移

创建了迁移文件：`migrations/008_add_path_role_and_unlock_by.sql`

包含内容：
- 添加新字段
- 添加约束检查
- 创建索引以优化查询
- 添加字段注释

执行迁移：
```bash
psql -U <username> -d <database> -f migrations/008_add_path_role_and_unlock_by.sql
```

### 3. 业务逻辑更新

#### 节点状态计算逻辑更新
在 `map_service.py` 的 `_NODE_STATUS_SQL` 中：

```sql
CASE
    WHEN tn.cocktail_id IN (SELECT cocktail_id FROM completed_cocktails) THEN 'completed'
    WHEN tn.is_entry_node OR COALESCE(pm.all_done, FALSE) THEN 'available'
    -- 新增：支线节点通过unlock_by解锁
    WHEN tn.path_role = 'side' AND tn.unlock_by IS NOT NULL 
         AND tn.unlock_by IN (SELECT cocktail_id FROM completed_cocktails) THEN 'available'
    ELSE 'locked'
END AS status
```

**解锁规则**：
1. **主线节点** (`path_role='main'`)：
   - 入口节点直接可用
   - 非入口节点需完成所有 prerequisite/progression 前置节点

2. **支线节点** (`path_role='side'`)：
   - 如果设置了 `unlock_by`，完成指定鸡尾酒后即可解锁
   - 如果未设置 `unlock_by`，遵循主线节点的解锁规则

#### 边状态计算逻辑
在 `get_theme_graph` 方法中新增边状态计算：

```python
# 计算边的解锁状态：from节点是available或completed时，边为unlocked
from_status = node_status_map.get(r.from_node_id, 'locked')
edge_status = 'unlocked' if from_status in ('available', 'completed') else 'locked'
```

**边状态规则**：
- `unlocked`：当 from 节点的状态为 `available` 或 `completed` 时
- `locked`：当 from 节点的状态为 `locked` 时

### 4. API 响应更新

#### 节点响应新增字段
```json
{
  "id": 466,
  "node_key": "c_1",
  "display_name_zh": "莫吉托",
  "cocktail_id": 127,
  "path_role": "main",
  "unlock_by": null,
  "status": "locked",
  ...
}
```

#### 边响应新增字段
```json
{
  "from": 465,
  "to": 466,
  "path_role": "main",
  "status": "unlocked"
}
```

## 使用示例

### 场景1：创建主线节点
```python
node = MapNode(
    theme_id=13,
    cocktail_id=277,
    node_key="c_0",
    is_entry_node=True,
    path_role="main",  # 主线节点
    unlock_by=None,
    reward_xp=10,
    sort_order=1
)
```

### 场景2：创建支线节点（通过购买解锁）
```python
side_node = MapNode(
    theme_id=13,
    cocktail_id=128,
    node_key="c_2",
    is_entry_node=False,
    path_role="side",      # 支线节点
    unlock_by=127,         # 完成莫吉托(ID=127)后解锁
    reward_xp=15,
    sort_order=3
)
```

### 场景3：创建主线/支线边
```python
# 主线边
main_edge = MapEdge(
    from_node_id=465,
    to_node_id=466,
    edge_type="progression",
    path_role="main"
)

# 支线边
side_edge = MapEdge(
    from_node_id=466,
    to_node_id=467,
    edge_type="variant",
    path_role="side"
)
```

## 前端集成指南

### 1. 区分主线/支线节点渲染
```javascript
function renderNode(node) {
  if (node.path_role === 'main') {
    // 主线样式：明亮、突出
    return <MainNode node={node} />;
  } else {
    // 支线样式：柔和、辅助
    return <SideNode node={node} />;
  }
}
```

### 2. 显示支线节点解锁提示
```javascript
function NodeDetail({ node }) {
  if (node.path_role === 'side' && node.unlock_by && node.status === 'locked') {
    return (
      <div>
        <p>完成 {getCocktailName(node.unlock_by)} 即可解锁此节点</p>
        <Button onClick={() => navigateToCocktail(node.unlock_by)}>
          查看解锁条件
        </Button>
      </div>
    );
  }
}
```

### 3. 根据边状态控制显示
```javascript
function renderEdge(edge) {
  if (edge.status === 'locked') {
    // 隐藏或显示为灰色虚线
    return <LockedEdge edge={edge} style={{ opacity: 0.3, strokeDasharray: '5,5' }} />;
  } else {
    // 正常显示
    return <UnlockedEdge edge={edge} />;
  }
}
```

### 4. 主线/支线边的差异化渲染
```javascript
function renderEdge(edge) {
  const baseStyle = edge.status === 'unlocked' ? {} : { opacity: 0.3 };
  
  if (edge.path_role === 'main') {
    // 主线边：粗线、实线
    return <Edge {...edge} style={{ ...baseStyle, strokeWidth: 3 }} />;
  } else {
    // 支线边：细线、虚线
    return <Edge {...edge} style={{ ...baseStyle, strokeWidth: 1, strokeDasharray: '3,3' }} />;
  }
}
```

## 数据验证

### 检查现有数据
```sql
-- 检查所有节点的path_role分布
SELECT path_role, COUNT(*) 
FROM map_nodes 
GROUP BY path_role;

-- 查找有unlock_by的支线节点
SELECT id, node_key, path_role, unlock_by
FROM map_nodes
WHERE path_role = 'side' AND unlock_by IS NOT NULL;

-- 检查所有边的path_role分布
SELECT path_role, edge_type, COUNT(*)
FROM map_edges
GROUP BY path_role, edge_type;
```

## 注意事项

1. **默认值**：所有现有节点和边的 `path_role` 默认为 `'main'`，保持向后兼容
2. **支线解锁**：`unlock_by` 字段可为 NULL，表示该支线节点使用标准的前置节点解锁逻辑
3. **边状态实时计算**：边的 `status` 是根据 from 节点状态动态计算的，不存储在数据库中
4. **跨主题共享**：鸡尾酒完成状态跨主题共享，`unlock_by` 指向的鸡尾酒在任何主题中完成都有效

## 后续优化建议

1. **批量导入工具**：创建脚本批量设置节点的 `path_role` 和 `unlock_by`
2. **编辑器支持**：在地图编辑器中添加主线/支线标记功能
3. **数据分析**：统计用户在主线和支线上的完成率差异
4. **推荐系统**：根据用户完成的节点，推荐相关的支线节点

## 版本信息

- 实现日期：2026-07-19
- API 版本：v1.5
- 数据库迁移版本：008
