# 前端接口变更通知 - 主题背景图

## 📅 变更日期
2026-08-01

## 🔄 变更接口

### 1. `GET /api/v1/map/user/stats` - 用户统计
### 2. `GET /api/v1/map/themes/{theme_id}` - 主题详情

## ✨ 新增字段
在主题相关对象中新增 `background_url` 字段，用于展示主题背景图。

### 字段说明
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `background_url` | `string \| null` | 主题背景图 URL，若未配置则为 `null` |

## 📊 返回数据示例

### 接口 1: `GET /api/v1/map/user/stats`

#### 请求
```http
GET /api/v1/map/user/stats
Authorization: Bearer <token>
```

#### 响应
```json
{
  "code": 0,
  "data": {
    "total_xp": 75,
    "themes": [
      {
        "theme_id": 14,
        "name_zh": "热带风情",
        "background_url": "http://115.191.50.177:5005/static/background/14.jpeg",
        "total": 27,
        "completed": 8,
        "is_unlocked": true,
        "progress_pct": 29
      }
    ]
  },
  "message": "ok"
}
```

---

### 接口 2: `GET /api/v1/map/themes/{theme_id}`

#### 请求
```http
GET /api/v1/map/themes/14
Authorization: Bearer <token>
```

#### 响应
```json
{
  "code": 0,
  "data": {
    "theme": {
      "id": 14,
      "slug": "tropical",
      "name_zh": "热带风情",
      "color_primary": "var(--emerald)",
      "color_secondary": null,
      "portal_cocktail_id": 235,
      "background_url": "http://115.191.50.177:5005/static/background/14.jpeg"
    },
    "nodes": [ /* 节点列表 */ ],
    "progression_edges": [ /* 前置边列表 */ ],
    "association_edges": [ /* 关联边列表 */ ],
    "variant_edges": [ /* 变体边列表 */ ],
    "archetype_bridge_edges": [ /* 桥接边列表 */ ]
  },
  "message": "ok"
}
```

## 💻 前端使用示例

### React
```jsx
function MapTheme({ theme }) {
  return (
    <div 
      style={{
        backgroundImage: theme.background_url 
          ? `url(${theme.background_url})` 
          : undefined,
        backgroundSize: 'cover',
        backgroundPosition: 'center'
      }}
    >
      <h2>{theme.name_zh}</h2>
      <p>完成度: {theme.progress_pct}%</p>
    </div>
  );
}
```

### Vue
```vue
<template>
  <div 
    class="map-theme"
    :style="{
      backgroundImage: theme.background_url 
        ? `url(${theme.background_url})` 
        : undefined
    }"
  >
    <h2>{{ theme.name_zh }}</h2>
    <p>完成度: {{ theme.progress_pct }}%</p>
  </div>
</template>
```

### 微信小程序
```html
<view class="map-theme" style="background-image: url({{theme.background_url}})">
  <text>{{theme.name_zh}}</text>
  <text>完成度: {{theme.progress_pct}}%</text>
</view>
```

## ⚠️ 注意事项

1. **向后兼容**：新增字段不影响现有功能，可选使用
2. **空值处理**：`background_url` 可能为 `null`，前端需做空值判断
3. **图片格式**：支持 `.jpeg` 和 `.png` 格式
4. **当前覆盖**：目前已配置主题 14、15、16 的背景图

## 📞 联系方式
如有疑问，请联系后端团队
