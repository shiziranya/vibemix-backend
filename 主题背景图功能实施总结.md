# 主题背景图功能实施总结

## 📋 功能概述

为地图探索功能的每个主题配置背景图片，通过 `/api/v1/map/user/stats` 接口返回背景图 URL，供前端在用户进入地图时展示。

---

## ✅ 实施内容

### 1. 图片存储

**存储路径**: `/opt/vibemix/vibemix-backend/app/static/background/`

**已上传的主题背景图**:
- `14.jpeg` - 热带风情 (337 KB)
- `15.jpeg` - 航海时代 (550 KB)
- `16.png` - 禁酒令 (6.4 MB)

**访问 URL 格式**:
```
http://115.191.50.177:5005/static/background/{theme_id}.{ext}
```

### 2. 数据库更新

**表**: `map_themes`  
**字段**: `cover_url` (已存在，TEXT 类型)

**更新的记录**:
```sql
UPDATE map_themes SET cover_url = 'http://115.191.50.177:5005/static/background/14.jpeg' WHERE id = 14;
UPDATE map_themes SET cover_url = 'http://115.191.50.177:5005/static/background/15.jpeg' WHERE id = 15;
UPDATE map_themes SET cover_url = 'http://115.191.50.177:5005/static/background/16.png' WHERE id = 16;
```

### 3. 代码修改

**文件**: `app/services/map_service.py`

**修改内容**:

#### SQL 查询 (第699-713行)
- 在 SELECT 语句中添加 `t.cover_url`
- 在 GROUP BY 语句中添加 `t.cover_url`

```python
SELECT
    t.id AS theme_id,
    t.name_zh,
    t.cover_url,  -- 新增
    COUNT(mn.id) AS total,
    ...
GROUP BY t.id, t.name_zh, t.cover_url, t.sort_order, tu.is_unlocked  -- 新增 t.cover_url
```

#### 返回数据 (第717-727行)
- 在主题字典中添加 `background_url` 字段

```python
themes = [
    {
        "theme_id": r.theme_id,
        "name_zh": r.name_zh,
        "background_url": r.cover_url,  -- 新增
        "total": r.total,
        "completed": r.completed,
        "is_unlocked": r.is_unlocked,
        "progress_pct": round(r.completed / r.total * 100) if r.total else 0,
    }
    for r in rows
]
```

---

## 📊 API 接口测试

### 请求示例

```bash
GET http://115.191.50.177:5005/api/v1/map/user/stats
Authorization: Bearer <JWT_TOKEN>
```

### 返回示例

```json
{
  "code": 0,
  "data": {
    "total_xp": 0,
    "themes": [
      {
        "theme_id": 13,
        "name_zh": "经典基石",
        "background_url": null,
        "total": 25,
        "completed": 0,
        "is_unlocked": true,
        "progress_pct": 0
      },
      {
        "theme_id": 14,
        "name_zh": "热带风情",
        "background_url": "http://115.191.50.177:5005/static/background/14.jpeg",
        "total": 27,
        "completed": 0,
        "is_unlocked": false,
        "progress_pct": 0
      },
      {
        "theme_id": 15,
        "name_zh": "航海时代",
        "background_url": "http://115.191.50.177:5005/static/background/15.jpeg",
        "total": 14,
        "completed": 0,
        "is_unlocked": false,
        "progress_pct": 0
      },
      {
        "theme_id": 16,
        "name_zh": "禁酒令",
        "background_url": "http://115.191.50.177:5005/static/background/16.png",
        "total": 33,
        "completed": 0,
        "is_unlocked": false,
        "progress_pct": 0
      }
    ]
  },
  "message": "ok"
}
```

### 验证结果

| 主题 ID | 主题名称 | 背景图状态 | URL |
|---------|---------|-----------|-----|
| 13 | 经典基石 | ✗ 未设置 | `null` |
| 14 | 热带风情 | ✓ 已设置 | `http://115.191.50.177:5005/static/background/14.jpeg` |
| 15 | 航海时代 | ✓ 已设置 | `http://115.191.50.177:5005/static/background/15.jpeg` |
| 16 | 禁酒令 | ✓ 已设置 | `http://115.191.50.177:5005/static/background/16.png` |
| 17 | 深夜优雅 | ✗ 未设置 | `null` |
| 18 | 派对狂欢 | ✗ 未设置 | `null` |
| 19 | 慵懒时光 | ✗ 未设置 | `null` |

**图片可访问性测试**: ✅ 所有背景图均返回 HTTP 200 状态码

---

## 🎯 前端使用指南

### 基础用法

```javascript
// 获取用户统计数据
const response = await fetch('http://115.191.50.177:5005/api/v1/map/user/stats', {
  headers: {
    'Authorization': `Bearer ${token}`
  }
});

const data = await response.json();

// 遍历主题并展示背景图
data.data.themes.forEach(theme => {
  if (theme.background_url) {
    // 设置地图背景
    setMapBackground(theme.background_url);
  } else {
    // 使用默认背景或渐变色
    setMapBackground(theme.color_primary);
  }
});
```

### React 示例

```jsx
function MapTheme({ theme }) {
  return (
    <div 
      className="map-container"
      style={{
        backgroundImage: theme.background_url 
          ? `url(${theme.background_url})` 
          : 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
        backgroundSize: 'cover',
        backgroundPosition: 'center'
      }}
    >
      <h2>{theme.name_zh}</h2>
      <p>进度: {theme.progress_pct}%</p>
    </div>
  );
}
```

---

## 📝 待办事项

### 短期
- [ ] 为其他主题 (13, 17, 18, 19) 添加背景图
- [ ] 优化图片 16.png (当前 6.4MB，建议压缩至 < 1MB)

### 长期
- [ ] 考虑迁移到 Supabase Storage (CDN 加速)
- [ ] 支持 WebP 格式以减小文件大小
- [ ] 添加图片懒加载和预加载策略

---

## 🔧 维护指南

### 添加新主题背景图

1. **上传图片**:
   ```bash
   cp new-theme.jpg /opt/vibemix/vibemix-backend/app/static/background/{theme_id}.jpg
   ```

2. **更新数据库**:
   ```sql
   UPDATE map_themes 
   SET cover_url = 'http://115.191.50.177:5005/static/background/{theme_id}.jpg'
   WHERE id = {theme_id};
   ```

3. **验证**:
   ```bash
   curl -I http://115.191.50.177:5005/static/background/{theme_id}.jpg
   ```

### 图片规格建议

- **格式**: JPEG (照片) 或 PNG (图形)
- **尺寸**: 1920×1080 或 2048×1024
- **大小**: < 500 KB (建议压缩)
- **命名**: `{theme_id}.{ext}`

---

## 📅 实施日期

**日期**: 2026-08-01  
**实施人**: Cursor AI Agent  
**版本**: v1.0

---

## ✨ 总结

本次更新成功为地图探索功能添加了主题背景图支持，现已覆盖 3 个主题（14、15、16）。前端可以通过 `/api/v1/map/user/stats` 接口获取每个主题的 `background_url` 字段，并在地图展示时使用。

接口已测试通过，所有图片均可正常访问，功能已部署并生效。
